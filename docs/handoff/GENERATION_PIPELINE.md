# 生图链路专题

## 当前合同

任务按“产品事实 -> 基础分镜 -> 创意导演 -> 逐图提示词 -> 生成前审核 -> 供应商调度 -> 生成后审核/定向返工 -> 拼接与打包”执行。

产品事实来自本次参考图、原始商品名、用户卖点、确认参数、平台和语言。案例库、旧任务、历史提示词只能提供视觉方法，不能提供商品事实。

套图数量是后端受控预设，不接受任意张数。默认 `standard-5-8` 为 5+8；`compact-1-2`、`compact-2-3`、`compact-3-4` 为轻量验证包，不替代完整成交叙事。预设 ID 和数量进入需求模板、任务元数据和状态；未知 ID 返回 400，历史任务缺省为标准套图。

本地智能模板、模型扩写质量门和逐屏规划都必须按当前预设生成并验证对应数量，不能出现“模板头部写 3+4、规划正文却仍是 5+8”的错配。

生图比例是独立的任务级受控预设。公开档位为 `ecommerce-standard`（主图 1:1/详情页 9:16，默认）和 `portrait-main`（主图 3:4/详情页 9:16）；未知 ID 返回 400。选择必须进入扩写、任务元数据、状态、逐图提示词、供应商请求、账本指纹、输出清单和尺寸审核；旧任务按已知 `suiteRatio` 恢复，否则回退默认比例。

图片清晰度公开 `1k`、`2k`、`4k`，默认 `2k`；主图宽度分别为 1024、2048、4096，按所选 1:1 或 3:4 计算高度，详情页保持 9:16。`720p` 只兼容旧任务，不接受新 Web 提交。

English 及基于 English 适配的其他语言，其可见展示名必须由原始名称和识图事实支持。强身份词（例如具体原料或剂型）没有可信证据时使用中性品类名；扩写质量门和生成前审核都会检查名称与识图证据的一致性。

输出语言由 `src/output-language-profiles.mjs` 统一控制。公开支持简体中文、English、日本語、한국어、Español、Français、Deutsch、Italiano、Português、العربية；别名先规范化，未知值返回 400。新增 8 种语言沿用 English 的类目推理、卖点证明、逐屏结构和内部源文案，再在最终可见文案合同中本地化为目标语言；源英文不得渲染，阿拉伯语额外要求 RTL、右对齐阅读层级和正确连字。

平台由 `src/platform-style-profiles.mjs` 统一注册。新任务只公开国内通用、国外通用，未知或旧专属平台值返回 400；旧八个平台配置仅供历史任务读取与续跑，不进入健康接口和前端下拉。平台选择继续进入规则路由、需求扩写、创意方向和最终逐图提示词。

鸭形机械玩具属于独立的“机械鸭拼装互动玩具”身份，优先于通用 AI 机器人分类；其英文展示名为 `Articulated Duck Robot Toy`。只可使用参考图可见的鸭头、侧眼、关节、脚部、图案和手部/小球互动作为证据，不能把“机器人”一词自动扩大为 AI、语音、联网、灯光、行走或套装数量承诺。

## 逐图要求

- 标准套图的 13 张各有唯一销售角色；轻量套图按连续的主图和详情页前段承担商品首图、核心卖点、核心主张、顾虑回应和卖点证明。1+2、2+3、3+4 均从这条连续叙事交付，不生成后再丢图。用户卖点形成覆盖矩阵。
- 当前画面任务放在提示词前部，公共约束压缩为必要条款。
- 提示词使用结构化长度预算：当前帧执行单和文案/画布/禁用项属于必保留区，可选产品上下文和旧提示词片段按完整行压缩；禁止直接截断完整提示词。
- 最终提示词中的新增文字、标点、标签及文字底板必须完整留在横向 12%–88%、纵向 10%–90% 安全区，文字组宽度不超过画布 42%；右侧文字也保持左对齐，长标题按语义换行并缩小字号，禁止截断、挤压、拆字或贴边。
- 每张至少明确：产品是否/如何出场、产品状态、场景、人物/动作/道具、镜头构图、光线材质、可见证据、精确文案、与邻图去重要求。
- “高级、真实、丰富”不是证据。证据必须是可见动作、状态对比、结构近景、真实互动、尺度关系或合理视觉隐喻。
- 平台风格与语言分别注入。两个公开平台都可组合任一受支持语言；平台只控制构图气质、信息密度、视觉节奏和禁用项，新增营销文案必须全套统一为用户选择的语言。

## 排障顺序

1. 查看任务的 `任务信息.json`/状态记录，确认产品、平台、语言和参考图属于本次任务。
2. 查看 `creative-plan.json`，确认卖点覆盖、逐图场景和去重是否已经具体。
3. 查看 `prompts.json`，确认逐图任务前置、无历史品类词、长度合理、文案语言正确。
4. 查看审核结果与失败日志，区分提示词合同失败、供应商失败、尺寸异常、视觉审核失败和前端未刷新。
5. 只修真正负责该问题的层；不要用更长公共提示词掩盖分镜或调度缺陷。

## 输出审计与返工

- 视觉审计响应必须覆盖全部预期 `role/index`；记录 `responseItemCount` 与 `matchedItemCount` 便于区分图片失败和协议缺项。
- 审计未返回某张图片时只标记人工复核，不得把协议缺项当作可执行质量失败自动重生。
- 对已经落盘的成品可运行 `npm run audit:output -- --dir <成品目录>`，只调用识图审计，不重新生图。
- 经用户授权后，可追加 `--retry-failed`，仅替换审计确认失败的图片；候选图先通过尺寸/文件质量校验，再覆盖原图，随后重建总览、详情长图、审计文件和压缩包。
- 视觉审核发现文字越界时，首条原因使用 `TEXT_BOUNDARY_VIOLATION:`，并说明边缘与文字区域。第一次视觉返工仍有该错误时，只对仍不合格的图片再执行一次保守文字布局返工：放弃右贴边构图，将文字组移到左上或左侧，安全区收紧为 15%–85% × 12%–88%，最大宽度降为 36%；已合格图片不得重复生成。

## 交付速度合同

- 所选预设的主图按索引优先进入供应商队列；标准套图的 5 路首发槽位由主图占用，后续空闲槽位立即由详情页补齐。首张主图落盘即发出结构化进度事件，Web 可在最终审核和打包前预览。
- 缺图恢复与可行动的视觉审核返工使用独立的受限并行队列，当前默认各 2 路。视觉返工先生成到候选文件，验证成功后才替换原图。
- 首轮整批零结果时先冷却并串行探测一张；只有探针成功才继续 2 路补图。探针仍失败时结束当前子流程，由持久化顶层任务按有限退避恢复，避免对容量不足的供应商同时重提整批图片。
- 生成器持续报告阶段、完成数、主图/详情页分别完成数、重试、限流与首图耗时；后台质检、返工、拼接和打包不得抹掉已生成预览。

## 修改边界

- 规则文件负责稳定平台/语言约束；品类事实和逐图策略放在代码结构层，不向公共规则堆具体商品示例。
- 修改 `src/openai-image-generator.ts` 前先定位函数与测试，避免无关重构。
- 真实付费生成只有在用户明确要求生成/返工时执行。代码验证优先使用测试和小规模工作流。
- 完成后至少运行相关测试、类型检查；改变预设、数量、分镜或输出合同的链路时运行全量测试。
