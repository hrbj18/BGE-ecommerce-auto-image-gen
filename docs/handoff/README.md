# 项目交接索引

默认只读根目录 `AGENTS.md` 和 `CURRENT_STATUS.md`。新电脑或新 Codex 首次接手再读 `FIRST_RUN.md`；生图链路读 `GENERATION_PIPELINE.md`；Web/局域网读 `WEB_APP.md`；模型/API 读 `MODEL_PROVIDERS.md`。每次只选当前任务需要的专题，需要稳定约束、决策理由或命令入口时再读对应文件。

不要默认读取旧接手说明、历次优化指导、历史任务目录或提示词素材库。它们是证据，不是现行规则。

当前任务入口：`CURRENT_STATUS.md`。

审计命令：

```powershell
pnpm run handoff:audit
```
